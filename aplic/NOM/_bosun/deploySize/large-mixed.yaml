# This is the resource specification for Large size (classic deployment)
global:
  nom:
    deploySize: large

nommetricstransform:
  baseline:
    jvmMax: "5120m"
  deployment:
    replicas: 3
  resources:
    limits:
      cpu: "7.0"
      memory: "8192Mi"

itomdipulsar:
  bookkeeper:
    volumes:
      ledgers:
        size: "280Gi"
