apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-example.fullname" . }}
  labels:
    {{- include "nodejs-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodejs-example.selectorLabels" . | nindent 4 }}
